1. Adjust any settings in greedy_projection.cpp.
2. Make sure that Points.pcd is set up correctly (spaces instead of commas).
3. cd into the build directory and "cmake .."
4. "make"
5. Move ./greedy_projection up a level to Clouds/
6. Run as "./greedy_projection"
